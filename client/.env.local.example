NEXT_PUBLIC_API_URL=http://localhost:5000

# Coordinator access token — the /c/[token] URL segment.
# Change this to any hard-to-guess string; share the full URL with coordinators only.
NEXT_PUBLIC_COORDINATOR_TOKEN=
